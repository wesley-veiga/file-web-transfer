
!.env.example
# Ambiente
# Dependências
# Expo / builds
# Sistema / editor
# Testes
*.jks
*.key
*.log
*.mobileprovision
*.p12
*.p8
.DS_Store
.env*
.expo/
android/
android/app/build/
coverage/
dist/
ios/
ios/build/
node_modules/
# sem barra no final também: cobre o caso de node_modules ser um symlink
# (ex.: worktrees que compartilham node_modules do checkout principal),
# que a regra com barra acima não cobre — symlink não conta como diretório
# para fins de match do gitignore.
node_modules
web-build/

# Husky (regenerado por `npm install` via script "prepare")
.husky/_

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
